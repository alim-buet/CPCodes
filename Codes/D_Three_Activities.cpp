#include <bits/stdc++.h>
using namespace std;
#define yes     cout << "YES" << endl;
#define no      cout << "NO" << endl;
#define vi      vector<int>
#define ll      long long
#define vl      vector<long long>
#define fori    for (int i = 0; i < n; i++) { cin >> arr[i]; }

int main() {
    int t;
    cin >> t;
    while (t--) {
        int n;cin >> n;
        vl skiing(n), movies(n), games(n);
        
        for (int i = 0;i < n;i++) cin >> skiing[i];
        for (int i = 0;i < n;i++) cin >> movies[i];
        for (int i = 0;i < n;i++) cin >> games[i];


    }
    return 0;
}